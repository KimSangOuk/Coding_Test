{ pkgs }: {
  deps = [
    pkgs.n,m=map(int,input().split())graph=dict()cost=dict()# 유토피아 세운 사람 포함indegree=dict()first=input()cost[first]=1indegree[first]=0for _ in range(n):  now,father,mother=map(str,input().split())  if father not in graph:    graph[father]=[]  if mother not in graph:    graph[mother]=[]  graph[father].append(now)  graph[mother].append(now)  if father not in cost:    cost[father]=0  if mother not in cost:    cost[mother]=0  if father not in indegree:    indegree[father]=0  if mother not in indegree:    indegree[mother]=0  cost[now]=0  indegree[now]=2candidate=[]for _ in range(m):  name=input()  if name not in cost:    cost[name]=0  if name not in indegree:    indegree[name]=0  candidate.append(name)# print(candidate)def topology_sort():  q=deque([])  result=copy.deepcopy(cost)  for i in indegree.keys():    if indegree[i]==0:      q.append(i)  # print(result)  while q:    now=q.popleft()    if now not in graph:      continue    for child in graph[now]:      # print(now,child,result[now])      result[child]+=float(result[now]/2)      indegree[child]-=1      if indegree[child]==0:        q.append(child)  # print(result)    answer=""  min_value=1  for c in candidate:    # print(result[c])    if 0<result[c]<=min_value:      min_value=result[c]      answer=c  print(answer)topology_sort()
    pkgs.mailutils
    pkgs.xorg.xorgserver
    pkgs.n,k=map(int,input().split())graph=[]for i in range(n):  graph.append(list(map(int,sys.stdin.readline().split())))queue=deque()new_arr=[]for i in range(n):  for j in range(n):    if graph[i][j]!=0:      new_arr.append((graph[i][j],i,j))queue.append(new_arr)s,x_a,y_a=map(int,sys.stdin.readline().split())dx=[0,-1,0,1]dy=[-1,0,1,0]for ms in range(s):  arr=queue.popleft()  new_arr=[]  for d,x,y in arr:    for i in range(4):      nx=x+dx[i]      ny=y+dy[i]      if nx>=0 and nx<n and ny>=0 and ny<n and graph[nx][ny]==0:        graph[nx][ny]=d        new_arr.append((d,nx,ny))  new_arr.sort()  queue.append(new_arr)  # for i in range(n):  #   print(graph[i])print(graph[x_a-1][y_a-1])
  ];
}